<?php echo "Inside test_include.php"; ?>

<?php
    echo "Testing PHP execution2"
    // include 'queries.php';

    // $address = isset($_GET['address']) ? $_GET['address'] : null;
    // $property = getPropertyDetails($address);
    // // var_dump($property);

    // // if (!$property) {
    // //     die("Property not found.");
    // // }

    // $addresses = getAddresses();
    // closeConnection();
?>